{
    "name": "test-network-${ORG}",
    "version": "1.0.0",
    "client": {
        "organization": "${ORG}",
        "connection": {
            "timeout": {
                "peer": {
                    "endorser": "300"
                }
            }
        }
    },
    "organizations": {
        "${ORG}": {
            "mspid": "${ORG}MSP",
            "peers": [
                "peer0.${ORG}.coedinus.com"
            ],
            "certificateAuthorities": [
                "ca.${ORG}.coedinus.com"
            ]
        }
    },
    "peers": {
        "peer0.${ORG}.coedinus.com": {
            "url": "grpcs://peer0.${ORG}.coedinus.com:${P0PORT}",
            "tlsCACerts": {
                "pem": "${PEERPEM}"
            },
            "grpcOptions": {
                "ssl-target-name-override": "peer0.${ORG}.coedinus.com",
                "hostnameOverride": "peer0.${ORG}.coedinus.com"
            }
        }
    },
    "certificateAuthorities": {
        "ca.${ORG}.coedinus.com": {
            "url": "https://ca.${ORG}.coedinus.com:${CAPORT}",
            "caName": "ca-${ORG}",
            "tlsCACerts": {
                "pem": ["${CAPEM}"]
            },
            "httpOptions": {
                "verify": false
            }
        }
    }
}
